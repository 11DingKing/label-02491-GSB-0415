# 购物车金额计算Bug修复记录

## 修复内容概述
修复了用户加入多件同一商品后，结算页面显示总金额与实际不一致的问题，确保金额计算准确性。

## 修改文件列表

### 1. `/backend/src/main/java/com/agrishop/controller/CartController.java`
#### 修改内容：
- 导入`com.agrishop.common.Log`注解类
- 为所有接口方法添加`@Log`操作日志注解：
  - `/list`接口：添加`@Log(module = "购物车管理", action = "查询购物车列表")`
  - `/count`接口：添加`@Log(module = "购物车管理", action = "查询购物车数量")`
  - `/add`接口：添加`@Log(module = "购物车管理", action = "添加商品到购物车")`
  - `/update`接口：添加`@Log(module = "购物车管理", action = "修改购物车商品数量")`
  - `/remove`接口：添加`@Log(module = "购物车管理", action = "删除购物车商品")`
  - `/clear`接口：添加`@Log(module = "购物车管理", action = "清空购物车")`

### 2. `/backend/src/main/webapp/WEB-INF/views/cart.jsp`
#### 修改内容：
- 优化前端`calcTotal()`金额计算逻辑：
  - 将原来直接浮点乘法计算改为转换为整数分计算，避免JavaScript浮点精度问题
  - 计算方式：`总金额(分) = 单价(元) * 100 * 数量`，最后转换为元显示
  - 确保金额计算精度，避免出现0.1+0.2=0.30000000000000004这类精度问题

### 3. `/backend/src/main/java/com/agrishop/service/OrderService.java`
#### 验证说明：
- 后端金额计算逻辑已使用`BigDecimal`进行计算，符合要求
- 订单创建时遍历购物车项，通过`g.getPrice().multiply(BigDecimal.valueOf(c.getQuantity()))`计算每个商品小计，累加得到总金额
- 所有计算均使用BigDecimal精确运算，无double/float类型计算问题

## 修复效果
1. 前端购物车页面金额计算不会再出现精度误差，多件商品总价显示正确
2. 后端订单创建时使用BigDecimal计算，保证金额准确性
3. 所有CartController接口都添加了操作日志记录，方便审计
4. 用户修改商品数量后，页面刷新重新计算金额，确保数据同步

## 注意事项
- 后续所有涉及金额计算的地方，后端必须统一使用BigDecimal类型
- 前端金额计算建议都转换为分进行整数运算，避免浮点精度问题
