# 农产品销售系统购物车结算Bug修复记录

## 问题描述

用户加入多件同一商品后，结算页面显示的总金额和实际不一致，数量变化后金额没有同步更新。

## 修改文件列表

### 1. CartController.java

**文件路径**: `backend/src/main/java/com/agrishop/controller/CartController.java`

**修改内容**:

- 为所有6个方法添加`@Log`注解记录操作日志
- 涉及方法：
  - `list()` - 查询购物车列表
  - `add()` - 添加购物车
  - `update()` - 更新购物车数量
  - `remove()` - 删除购物车项
  - `clear()` - 清空购物车
  - `count()` - 查询购物车数量

**代码示例**:

```java
@GetMapping("/list")
@Log(module = "购物车管理", action = "查询购物车列表")
public Result<?> list(HttpServletRequest request) {
    Long userId = (Long) request.getAttribute("userId");
    return Result.ok(cartService.listByUser(userId));
}
```

---

### 2. OrderController.java

**文件路径**: `backend/src/main/java/com/agrishop/controller/OrderController.java`

**修改内容**:

- 为缺少`@Log`注解的方法添加日志注解
- 涉及方法：
  - `list()` - 查询订单列表
  - `detail()` - 查询订单详情

**代码示例**:

```java
@GetMapping("/list")
@Log(module = "订单管理", action = "查询订单列表")
public Result<?> list(HttpServletRequest request,
                      @RequestParam(defaultValue = "1") int page,
                      @RequestParam(defaultValue = "10") int size,
                      Integer status) {
    // ...
}
```

---

### 3. OrderService.java

**文件路径**: `backend/src/main/java/com/agrishop/service/OrderService.java`

**修改内容**:

1. **修复价格一致性问题**：
   - 原问题：创建订单时使用购物车关联查询的商品价格（`c.getGoods()`），该价格可能不是最新价格
   - 修复：改为直接从`goodsDao`查询最新价格（`goodsDao.selectById(c.getGoodsId())`）

2. **优化数据库查询**：
   - 使用`HashMap`缓存查询到的商品信息，避免第二次循环时重复查询数据库
   - 第一次循环时查询并缓存商品信息，第二次循环直接从缓存获取

3. **确保金额计算精度**：
   - 所有金额计算都使用`BigDecimal`进行，避免浮点数精度问题
   - 价格乘法：`g.getPrice().multiply(BigDecimal.valueOf(c.getQuantity()))`
   - 金额累加：`total = total.add(...)`

**代码示例**:

```java
BigDecimal total = BigDecimal.ZERO;
java.util.Map<Long, Goods> goodsMap = new java.util.HashMap<>();
for (Cart c : carts) {
    Goods g = goodsDao.selectById(c.getGoodsId());
    if (g == null || g.getStatus() != 1) throw new BizException("商品[" + c.getGoodsId() + "]已下架");
    if (g.getStock() < c.getQuantity()) throw new BizException("商品[" + g.getName() + "]库存不足");
    goodsMap.put(c.getGoodsId(), g);
    total = total.add(g.getPrice().multiply(BigDecimal.valueOf(c.getQuantity())));
}
```

---

### 4. cart.jsp

**文件路径**: `backend/src/main/webapp/WEB-INF/views/cart.jsp`

**修改内容**:

1. **修复前端金额计算精度问题**：
   - 原问题：直接使用浮点数乘法`g.price * c.quantity`，可能产生精度误差（如`0.1 + 0.2 = 0.30000000000000004`）
   - 修复：使用整数运算减少浮点误差，先乘以100转为整数计算后再除以100

2. **商品小计显示优化**：
   - 原代码：`g.price * c.quantity`
   - 修复后：`((g.price * 100 * c.quantity) / 100).toFixed(2)`

3. **总金额计算优化**：
   - 原代码：`total += price * qty`
   - 修复后：`total = (total * 100 + price * 100 * qty) / 100`

4. **购物车徽标同步更新**：
   - 在`loadCart()`函数中添加`refreshCartBadge()`调用，确保购物车数量变化后导航栏徽标同步更新

5. **选中状态保持**：
   - 修改数量或删除商品前先保存选中状态
   - 重新加载购物车后恢复选中状态
   - 恢复后重新计算总金额
   - 新增函数：`getSelectedIds()`、`restoreSelection()`
   - `loadCart()`函数改为返回Promise，支持链式调用

**代码示例**:

```javascript
// 商品小计显示
'<div class="item-price">¥' +
  ((g.price * 100 * c.quantity) / 100).toFixed(2) +
  "</div>";

// 总金额计算
function calcTotal() {
  var total = 0;
  $(".cart-check:checked").each(function () {
    var price = parseFloat($(this).data("price"));
    var qty = parseInt($(this).data("qty"));
    total = (total * 100 + price * 100 * qty) / 100;
  });
  $("#totalPrice").text("¥" + total.toFixed(2));
}
```

---

### 5. checkout.jsp

**文件路径**: `backend/src/main/webapp/WEB-INF/views/checkout.jsp`

**修改内容**:

1. **新增商品清单显示**：
   - 添加商品列表展示区域，显示用户选中的所有商品
   - 显示每个商品的图片、名称、单价、数量和小计

2. **新增总数量和总金额显示**：
   - 显示选中商品的总件数
   - 显示订单的总金额
   - 使用整数运算确保金额计算精度

3. **新增JavaScript函数**：
   - `loadCartItems()`: 加载购物车数据并筛选选中的商品
   - `renderGoodsList()`: 渲染商品列表并计算总数量和总金额

**代码示例**:

```javascript
function loadCartItems() {
  api("/api/cart/list").then(function (list) {
    cartItems = list.filter(function (c) {
      return cartIds.indexOf(c.id) > -1;
    });
    renderGoodsList();
  });
}
function renderGoodsList() {
  var h = "",
    totalQty = 0,
    totalPrice = 0;
  cartItems.forEach(function (c) {
    var g = c.goods || {};
    var subtotal = (g.price * 100 * c.quantity) / 100;
    totalQty += c.quantity;
    totalPrice = (totalPrice * 100 + subtotal * 100) / 100;
    // ... 渲染HTML
  });
  $("#totalQty").text(totalQty);
  $("#totalPrice").text("¥" + totalPrice.toFixed(2));
}
```

---

## 修复总结

### 已解决的问题

1. ✅ 前端购物车页面金额计算精度问题（通过整数运算避免浮点数误差）
2. ✅ CartController所有方法添加@Log日志注解
3. ✅ OrderController缺少@Log注解的方法补充添加
4. ✅ OrderService.createOrder方法价格一致性问题（改为查询实时商品价格而非使用购物车关联的缓存数据）
5. ✅ OrderService重复查询数据库优化（使用HashMap缓存查询到的商品信息）
6. ✅ 所有后端金额计算都使用BigDecimal，避免精度丢失
7. ✅ 购物车徽标同步更新问题（在loadCart()中调用refreshCartBadge()）
8. ✅ 结算页面商品数量和金额显示问题（新增商品清单、总数量、总金额显示）
9. ✅ 修改商品数量后选中状态丢失问题（保存并恢复选中状态，重新计算金额）

### 技术要点

1. **金额计算精度**：后端使用`BigDecimal`，前端使用整数运算减少浮点误差
2. **数据一致性**：创建订单时查询实时价格，确保价格准确
3. **性能优化**：使用HashMap缓存，避免重复数据库查询
4. **操作日志**：所有涉及修改的方法都添加了@Log注解
5. **用户体验**：结算页面显示商品清单，让用户确认订单信息
