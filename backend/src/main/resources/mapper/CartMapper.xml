<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agrishop.dao.CartDao">
    <resultMap id="cartWithGoods" type="Cart">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="goodsId" column="goods_id"/>
        <result property="quantity" column="quantity"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <association property="goods" javaType="Goods">
            <id property="id" column="g_id"/>
            <result property="name" column="g_name"/>
            <result property="coverImg" column="g_cover_img"/>
            <result property="price" column="g_price"/>
            <result property="stock" column="g_stock"/>
            <result property="status" column="g_status"/>
        </association>
    </resultMap>

    <select id="selectCartWithGoods" resultMap="cartWithGoods">
        SELECT c.*, g.id AS g_id, g.name AS g_name, g.cover_img AS g_cover_img,
               g.price AS g_price, g.stock AS g_stock, g.status AS g_status
        FROM cart c LEFT JOIN goods g ON c.goods_id = g.id
        WHERE c.user_id = #{userId} ORDER BY c.created_at DESC
    </select>
    <select id="selectByUserAndGoods" resultType="Cart">
        SELECT * FROM cart WHERE user_id = #{userId} AND goods_id = #{goodsId}
    </select>
    <select id="selectByUserAndGoodsForUpdate" resultType="Cart">
        SELECT * FROM cart WHERE user_id = #{userId} AND goods_id = #{goodsId} FOR UPDATE
    </select>
    <select id="selectById" resultType="Cart">
        SELECT * FROM cart WHERE id = #{id}
    </select>
    <select id="selectByIds" resultMap="cartWithGoods">
        SELECT c.*, g.id AS g_id, g.name AS g_name, g.cover_img AS g_cover_img,
               g.price AS g_price, g.stock AS g_stock, g.status AS g_status
        FROM cart c LEFT JOIN goods g ON c.goods_id = g.id
        WHERE c.user_id = #{userId} AND c.id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart (user_id, goods_id, quantity) VALUES (#{userId}, #{goodsId}, #{quantity})
    </insert>
    <update id="updateById">
        UPDATE cart SET quantity = #{quantity} WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM cart WHERE id = #{id}
    </delete>
    <delete id="deleteByUserId">
        DELETE FROM cart WHERE user_id = #{userId}
    </delete>
    <delete id="deleteBatchIds">
        DELETE FROM cart WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>
    <select id="selectCountByUserId" resultType="long">
        SELECT COUNT(*) FROM cart WHERE user_id = #{userId}
    </select>
</mapper>
