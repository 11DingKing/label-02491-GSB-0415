<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agrishop.dao.OrderItemDao">
    <select id="selectByOrderId" resultType="OrderItem">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, goods_id, goods_name, goods_img, goods_price, quantity, subtotal)
        VALUES (#{orderId}, #{goodsId}, #{goodsName}, #{goodsImg}, #{goodsPrice}, #{quantity}, #{subtotal})
    </insert>
</mapper>
