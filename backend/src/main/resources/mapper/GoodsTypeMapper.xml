<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agrishop.dao.GoodsTypeDao">
    <select id="selectAll" resultType="GoodsType">
        SELECT * FROM goods_type ORDER BY sort_order ASC
    </select>
    <select id="selectAllEnabled" resultType="GoodsType">
        SELECT * FROM goods_type WHERE status = 1 ORDER BY sort_order ASC
    </select>
    <select id="selectById" resultType="GoodsType">
        SELECT * FROM goods_type WHERE id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO goods_type (name, sort_order, status) VALUES (#{name}, #{sortOrder}, #{status})
    </insert>
    <update id="updateById">
        UPDATE goods_type
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM goods_type WHERE id = #{id}
    </delete>
</mapper>
